import React from "react";
import Navbar from "./Navbar";

const Header = ({ logo, users }) => {
  // console.log(logo);
  // console.log(users);
  return (
    <div>
      <Navbar />
    </div>
  );
};

export default Header;
